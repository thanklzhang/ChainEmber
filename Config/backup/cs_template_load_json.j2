/*
 * generate by tool
*/

using System.Collections.Generic;
using System.Linq;
using LitJson;
using System.IO;

namespace Table
{
    public class {{ name }}Store : BaseStore<Table.{{ name }}>
    {
        internal override void Load()
        {

            List = JsonMapper.ToObject<List<Table.{{ name }}>>(File.ReadAllText("{{ name }}.json"));

            Dic = List.ToDictionary(info => { return info.Id; });
        }
    }

}
